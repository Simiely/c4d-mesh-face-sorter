# 更新日志（CHANGELOG）

本文件记录每个版本的变更。

## v2.0.6（当前版本）

### 重构
- 移除 `_original_modes` 状态快照依赖，显示全部/孤立改为无状态幂等操作——根治「关闭插件窗口重开后无法全部显示」问题
- 删除死代码（`_count_faces_recursive`、GUID 查找机制），列表项直接持有对象引用

### 加固
- dead atom 判断改用 `IsAlive()`
- `RestoreLayout` 调用 `Dialog.Restore()` 支持布局恢复

### 修复
- 导出报表区分「未刷新」与「场景无物体」两种提示
- 空场景点「显示全部」不再弹无意义提示

## v2.0.3

### 修复
- 多次独显不同对象后，显示全部无法恢复到初始状态（状态快照只在第一次独显时保存）
- 优化独显逻辑：支持多次切换独显对象

## v2.0.2

### 修复
- 孤立功能在老工程中无效（改用 `SetEditorMode` 控制可见性）
- 孤立功能保存原始状态，显示全部时正确恢复（不影响之前的隐藏操作）
- 同名对象导致的混乱（改用 GUID 查找对象）
- 扫描时只显示纯多边形对象（不再统计父级组对象的子级面数）

## v2.0.1

### 优化
- 图标加载逻辑，支持 PNG 透明图标
- 添加 `.gitignore`

## v2.0.0

### 新增
- 按面数和存储大小排序
- 孤立显示功能
- 删除空物体（安全删除）
- 导出 Markdown 报表
- 显示全部功能
